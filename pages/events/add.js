import Layout from '../../components/Layout'

const AddEventPage = () => {
    return (
        <Layout title="Add New Event">

            <h1>add Event</h1>

        </Layout>
    );
};

export default AddEventPage;
